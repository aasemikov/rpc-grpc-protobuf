# gRPC Dictionary Service

Микросервисный глоссарий терминов с использованием gRPC, Protocol Buffers и Docker.

## 🏗️ Архитектура

- **gRPC сервер**: Обрабатывает запросы к словарю на порту 50051
- **Flask фронтенд**: Веб-интерфейс на порту 5000
- **Protocol Buffers**: Определение API контракта

## Используемые технологии

- Язык программирования: Python 3.9
- Межсервисное взаимодействие: gRPC + Protocol Buffers (protobuf v4.25.3)
- Сервер: gRPC-сервер на базе grpcio и concurrent.futures
- Веб-интерфейс: Flask (v3.0.0)
- Сборка и развёртывание: Docker и Docker Compose
- Зависимости: grpcio, grpcio-tools, protobuf, sqlalchemy (в зависимостях, но не используется в текущей реализации), flask-cors, requests
- Контракт данных: определён в файле dictionary.proto, автоматически генерируются Python-классы для клиентов и сервера

## Использование базы данных
В текущей реализации постоянная внешняя база данных не используется. Все данные хранятся в оперативной памяти сервера в виде обычного Python-словаря (Dict[str, dict]). При запуске сервера загружаются предопределённые начальные термины (hardcoded). Это означает, что:

Данные не сохраняются между перезапусками сервера.
Приложение подходит для демонстрации, тестирования или временного использования, но не для production без доработки.

Данные, необходимые для выполнения запроса на добавление
Для добавления нового термина необходимо отправить POST-запрос на /api/terms с телом в формате JSON, содержащим три обязательных поля:

- term — строка, уникальное название термина (чувствительно к регистру);
- definition — строка, определение термина;
- category — строка, категория термина (например, "API", "Containerization").

Необязательные поля:
- related_terms — массив строк с названиями связанных терминов;
- source — строка, указывающая источник определения (например, "Google", "RFC 7231").

Например:
```json
{
  "term": "Kubernetes",
  "definition": "Система оркестрации контейнеров с открытым исходным кодом",
  "category": "Containerization",
  "related_terms": ["Docker", "Containers", "Orchestration"],
  "source": "Google"
}
```
